# Lint ignore list.
# One path substring per line, matched against project-relative paths
# (forward-slash separated). Lines starting with # are comments.
#
# __External* (UE5 World Partition external actors) is hardcoded
# in src/validator.py and is always skipped, regardless of this file.

# Examples:
# Marketplace
# ThirdParty
# _GENERATED
# InputGlyph
